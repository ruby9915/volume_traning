# 첫 관리자 계정 (v2, SETTING.MD §10.1) — 사용자 테이블이 비어 있을 때 한 번만 이 값으로 생성.
# 이후 비밀번호는 앱 설정 화면에서 바꾸며, 이 값은 더 이상 쓰이지 않는다.
ADMIN_USERNAME=admin
APP_PASSWORD=
JWT_SECRET=
DB_PATH=
TOKEN_TTL_DAYS=
# 클라우드 동기화 폴더(OneDrive 등) 경로. 비워두면 자동 백업 꺼짐
BACKUP_DIR=
